spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/taskle
    username: xhosh
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate

logging:
  level:
    root: INFO